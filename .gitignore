# Terraform
.terraform/
# lock ファイルは意図的に ignore（provider は root.hcl の version 制約で管理。
# パッチバージョンまで固定したくなったら live 側のみコミットに切り替える）
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
terraform.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terragrunt
.terragrunt-cache/

# Lambda zip (自動生成)
*.zip

# Secrets (絶対にコミットしない)
*.pem
*.key
*.p12
entra-id-metadata.xml   # SAML メタデータは .gitignore して別管理

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/

# Python bytecode（Lambda のローカルテストで生成される）
__pycache__/
*.pyc
